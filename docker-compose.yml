version: '3.8'

services:
  story-ai:
    build: .
    container_name: story-ai
    ports:
      - "${PORT:-50052}:${PORT:-50052}"
    volumes:
      - ./src:/app/src
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
